<div class="days">
  {{#each days as |d|}}
    {{calendar/calendar-day
      selectedMonth=selectedMonth
      day=d
      events=events
      selectedDay=selectedDay
      selectDay=selectDay
    }}
  {{/each}}
</div>
